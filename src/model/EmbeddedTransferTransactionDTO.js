/**
 * Catapult REST API Reference
 * No description provided (generated by Openapi Generator https://github.com/openapitools/openapi-generator)
 *
 * OpenAPI spec version: 0.7.15
 * 
 *
 * NOTE: This class is auto generated by OpenAPI Generator (https://openapi-generator.tech).
 * https://openapi-generator.tech
 * Do not edit the class manually.
 *
 */

import ApiClient from '../ApiClient';
import EmbeddedTransactionDTO from './EmbeddedTransactionDTO';
import MessageDTO from './MessageDTO';
import MosaicDTO from './MosaicDTO';
import TransferTransactionBodyDTO from './TransferTransactionBodyDTO';

/**
 * The EmbeddedTransferTransactionDTO model module.
 * @module model/EmbeddedTransferTransactionDTO
 * @version 0.7.15
 */
class EmbeddedTransferTransactionDTO {
    /**
     * Constructs a new <code>EmbeddedTransferTransactionDTO</code>.
     * @alias module:model/EmbeddedTransferTransactionDTO
     * @implements module:model/EmbeddedTransactionDTO
     * @implements module:model/TransferTransactionBodyDTO
     */
    constructor() { 
        EmbeddedTransactionDTO.initialize(this);TransferTransactionBodyDTO.initialize(this, recipient, mosaics, message);
        EmbeddedTransferTransactionDTO.initialize(this);
    }

    /**
     * Initializes the fields of this object.
     * This method is used by the constructors of any subclasses, in order to implement multiple inheritance (mix-ins).
     * Only for internal use.
     */
    static initialize(obj) { 
        obj['signer'] = signer;
        obj['version'] = version;
        obj['type'] = type;
        obj['max_fee'] = maxFee;
        obj['deadline'] = deadline;
        obj['recipient'] = recipient;
        obj['mosaics'] = mosaics;
        obj['message'] = message;
    }

    /**
     * Constructs a <code>EmbeddedTransferTransactionDTO</code> from a plain JavaScript object, optionally creating a new instance.
     * Copies all relevant properties from <code>data</code> to <code>obj</code> if supplied or a new instance if not.
     * @param {Object} data The plain JavaScript object bearing properties of interest.
     * @param {module:model/EmbeddedTransferTransactionDTO} obj Optional instance to populate.
     * @return {module:model/EmbeddedTransferTransactionDTO} The populated <code>EmbeddedTransferTransactionDTO</code> instance.
     */
    static constructFromObject(data, obj) {
        if (data) {
            obj = obj || new EmbeddedTransferTransactionDTO();
            EmbeddedTransactionDTO.constructFromObject(data, obj);
            TransferTransactionBodyDTO.constructFromObject(data, obj);

            if (data.hasOwnProperty('signer')) {
                obj['signer'] = ApiClient.convertToType(data['signer'], 'String');
            }
            if (data.hasOwnProperty('version')) {
                obj['version'] = ApiClient.convertToType(data['version'], 'Number');
            }
            if (data.hasOwnProperty('type')) {
                obj['type'] = ApiClient.convertToType(data['type'], Object);
            }
            if (data.hasOwnProperty('max_fee')) {
                obj['max_fee'] = ApiClient.convertToType(data['max_fee'], ['Number']);
            }
            if (data.hasOwnProperty('deadline')) {
                obj['deadline'] = ApiClient.convertToType(data['deadline'], ['Number']);
            }
            if (data.hasOwnProperty('recipient')) {
                obj['recipient'] = ApiClient.convertToType(data['recipient'], Object);
            }
            if (data.hasOwnProperty('mosaics')) {
                obj['mosaics'] = ApiClient.convertToType(data['mosaics'], [MosaicDTO]);
            }
            if (data.hasOwnProperty('message')) {
                obj['message'] = MessageDTO.constructFromObject(data['message']);
            }
        }
        return obj;
    }


}

/**
 * The public key of the entity signer formatted as hexadecimal.
 * @member {String} signer
 */
EmbeddedTransferTransactionDTO.prototype['signer'] = undefined;

/**
 * The entity version. The higher byte represents the network identifier: * 0x68 (MAIN_NET) - Public main network. * 0x98 (TEST_NET) - Public test network. * 0x60 (MIJIN) - Private network. * 0x90 (MIJIN_TEST) - Private test network. 
 * @member {Number} version
 */
EmbeddedTransferTransactionDTO.prototype['version'] = undefined;

/**
 * @member {Object} type
 */
EmbeddedTransferTransactionDTO.prototype['type'] = undefined;

/**
 * @member {Array.<Number>} max_fee
 */
EmbeddedTransferTransactionDTO.prototype['max_fee'] = undefined;

/**
 * @member {Array.<Number>} deadline
 */
EmbeddedTransferTransactionDTO.prototype['deadline'] = undefined;

/**
 * If the bit 0 of byte 0 is not set (like in 0x90), then it is a regular address. Else (e.g. 0x91) it represents a namespace id which starts at byte 1.
 * @member {Object} recipient
 */
EmbeddedTransferTransactionDTO.prototype['recipient'] = undefined;

/**
 * The array of mosics sent to the recipient. If the most significant bit of byte 0 is set, a namespaceId (alias) is used instead of a instead of a mosaicId corresponds to a mosaicId.
 * @member {Array.<module:model/MosaicDTO>} mosaics
 */
EmbeddedTransferTransactionDTO.prototype['mosaics'] = undefined;

/**
 * @member {module:model/MessageDTO} message
 */
EmbeddedTransferTransactionDTO.prototype['message'] = undefined;


// Implement EmbeddedTransactionDTO interface:
// Implement TransferTransactionBodyDTO interface:
/**
 * @member {Object} type
 */
TransferTransactionBodyDTO.prototype['type'] = undefined;
/**
 * If the bit 0 of byte 0 is not set (like in 0x90), then it is a regular address. Else (e.g. 0x91) it represents a namespace id which starts at byte 1.
 * @member {Object} recipient
 */
TransferTransactionBodyDTO.prototype['recipient'] = undefined;
/**
 * The array of mosics sent to the recipient. If the most significant bit of byte 0 is set, a namespaceId (alias) is used instead of a instead of a mosaicId corresponds to a mosaicId.
 * @member {Array.<module:model/MosaicDTO>} mosaics
 */
TransferTransactionBodyDTO.prototype['mosaics'] = undefined;
/**
 * @member {module:model/MessageDTO} message
 */
TransferTransactionBodyDTO.prototype['message'] = undefined;




export default EmbeddedTransferTransactionDTO;

